<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="Twitter Clone - one user, one page Twitter clone written using PHP, MySQL, Silex, and Twitter Bootstrap">
        <meta name="author" content="Robert C. McIntosh">    
        <title>Twitter Clone</title>
        <link href="/css/bootstrap.min.css" rel="stylesheet">    
        <!--[if lt IE 9]>
            <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
            <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->
    </head>

    <body>
        <div class="container">

            <div class="row">
                <div class="col-md-12">
                    {% for alert in app.session.flashbag.get('error') %}
                        <div class="alert alert-danger" role="alert">
                            <strong>{{ alert }}</strong>
                        </div>
                    {% endfor %}
                </div>
            </div>
            
            <div class="row">
                <div class="col-md-12">
                    <h2>Tweet Something:</h2>
                </div>
            </div>
            
            <div class="row">
                <div class="col-md-12">
                    <form action="/tweet" method="post">
                        {{ form_widget(form) }}
                        <input type="submit" name="submit" />
                    </form>
                </div>
            </div>

            <div class="row">
                <div class="col-md-12">
                    <h2>Tweets</h2>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <ul>
                        {% for tweet in tweets %}
                            <li>{{ tweet[1] }} - {{ tweet[0]}}</li>
                        {% endfor %}
                  </ul>
                </div>
            </div>
        </div>
        
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
        <script>window.jQuery || document.write('<script src="/js/jquery.min.js"><\/script>')</script>
        <script src="/js/bootstrap.min.js"></script>
    </body>
</html>
